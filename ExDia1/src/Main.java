import java.util.Arrays;
import java.util.Scanner;


public class Main {
    public static void main(String[] args) {

        // Inicializamos um vetor e um objeto do tipo Scanner (abordaremos mais no segundo dia)
        int vetor[] = new int[3];
        Scanner scanner = new Scanner(System.in);

        for (int i = 0; i < 3; i++) {
            System.out.println("Insira um número inteiro:");
            // ATIVIDADE nível 1: use o objeto scanner para ler a entrada do usuário
        }

        // ATIVIDADE nível 1: crie um loop para somar os números armazenados no vetor, e imprima o resultado

        /*
            ATIVIDADE 2: faça a soma do vetor sem utilizar loop, mas sim uma função do vetor
            Dica: algumas IDE's já sugerem a função; se não conseguir, pesquise sobre Arrays.stream()
        */ 
    }

}